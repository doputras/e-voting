name: tubesuts
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.3 <3.0.0'

dependencies:
  cloud_firestore: ^4.5.1
  firebase_auth: ^4.4.1
  firebase_core: ^2.9.0
  google_fonts: ^2.1.0
  image_picker: ^0.8.4+2
  flutter:
    sdk: flutter
  latlong2: ^0.8.2
  flutter_map: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
